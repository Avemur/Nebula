[package]
name = "nebula-control"
version.workspace = true
edition.workspace = true

[dependencies]
# Deliberately no `nebula-runtime`: the control plane never executes wasm, and
# depending on the engine to hash a few bytes would be backwards.
nebula-proto.workspace = true
tonic.workspace = true
axum.workspace = true
tokio.workspace = true
sha2.workspace = true
